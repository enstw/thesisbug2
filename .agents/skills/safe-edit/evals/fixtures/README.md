# safe-edit eval fixtures

Invented chapter; the citation keys are placeholders with no bib file. Copy `unit/` into a scratch course repo as `units/01-paper-eval/` before running.

Planted traps: the sentence 「監理機關要求銀行凍結帳戶」 occurs twice (an anchor on it alone is ambiguous — it must be extended with the adjacent `[@key]`), and 「風險隔離」 is a direct quotation that a terminology sweep must leave alone.
