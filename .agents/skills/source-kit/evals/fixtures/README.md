# source-kit eval fixtures

Everything here is invented (author, book, journal, reviewer). Copy `unit/` into a scratch course repo as `units/01-paper-eval/` and run the evals there, so audit lines are never appended inside the skill folder.

Planted defect: `<unit>/refs/harlow2019chokepoints.md` is byte-clean and page-marked, but it is a **review of** the cited book, not the book. The cited sentence in `paper.qmd` also overstates it: the reviewer says the claim is "asserted rather than demonstrated", while the draft says Harlow "proved" it with four cases.
