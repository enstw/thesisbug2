# 變更紀錄

> **不到必要不讀取。** 這是歷程，不是規則，也不是現況：寫作、審稿、跑閘門都不需要它。
> 只有在追溯某個決定或某輪數字的來歷時才查，而且用 `./fw log --find <keyword>`（只回傳 id、日期、標題與片段），找到後 `./fw log --show <id>` 取單條全文；不要整份讀，也不要直接 `grep`（單條可能數千字，一命中就是數 KB）。
> 規則與現況在 [`PROGRESS.md`](PROGRESS.md)。

（新→舊）

- [YYYY-MM-DD] - 建立工作項目
