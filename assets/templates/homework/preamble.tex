% === CJK & Font Setup ===
\usepackage{xeCJK}
\setmainfont[
  Path={{FW}}/assets/fonts/,
  Extension=.ttf,
  BoldFont=ENSFont-Bold
]{ENSFont-Regular}
\setCJKmainfont[
  Path={{FW}}/assets/fonts/,
  Extension=.ttf,
  BoldFont=ENSFont-Bold
]{ENSFont-Regular}

% === Chinese Line Breaking ===
\XeTeXlinebreaklocale "zh"
\XeTeXlinebreakskip = 0pt plus 1pt

% === Line Spacing ===
\usepackage{setspace}
\onehalfspacing

% === Page Margins ===
\usepackage[top=2.54cm, bottom=2.54cm, left=2.54cm, right=2.54cm]{geometry}

% === Header & Footer ===
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

% === One-liner Title: left=title, right=author+date ===
\makeatletter
\renewcommand{\maketitle}{%
  \noindent\textbf{\@title}\hfill\@author\quad\@date\par
  \vspace{0.5em}%
  \hrule
  \vspace{1em}%
}
\makeatother
